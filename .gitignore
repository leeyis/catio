node_modules/
dist/
src-tauri/target/
# JDBC sidecar Maven build output (regenerable via `mvn package`)
src-tauri/jdbc-plugin/target/
# local JDBC driver JARs used for integration testing (downloaded, not redistributed)
.test-drivers/
.DS_Store
# reference UI assets (design scratch, not part of the shipped product)
ref-ui/
# pixel-diff artifacts
diff.png
*-actual.png
*-diff.png
.gstack/

# git worktrees
.worktrees/

# superpowers brainstorm mockups
.superpowers/

# build / tooling logs (test & compile output, regenerable)
*.log

# internal design specs & agent instructions (not for the public repo)
docs/superpowers/
AGENTS.md
CLAUDE.md

# local integration-test scaffolding (docker compose / env notes)
deploy/

# misc local tooling state
.nezha/

# arm64 deb 构建产物
dist-deb/
